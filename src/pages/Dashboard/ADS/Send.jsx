import { useState, useEffect } from "react";
import { apiUrl } from "../../../config";
import { FaSearch } from "react-icons/fa";

export default function AdsMessages() {
  const [message, setMessage] = useState("");
  const [phone, setPhone] = useState("");
  const [bulkMode, setBulkMode] = useState(false);
  const [file, setFile] = useState(null);
  const [policyAccepted, setPolicyAccepted] = useState(false);

  const [history, setHistory] = useState([]);
  const [filterPhone, setFilterPhone] = useState("");
  const [filterFromDate, setFilterFromDate] = useState("");
  const [filterToDate, setFilterToDate] = useState("");

  const maxLength = 600;

  const handleFileChange = (e) => setFile(e.target.files[0]);

  const handleSubmit = async (e) => {
    e.preventDefault();

    if (!policyAccepted) return alert("�?�?�?�? �?�?�?�? �?�?�?�?�? �?�?�?�?���? �?�?�? �?�?�?�?�?�?�?");
    if (!bulkMode && !phone.trim()) return alert("�?�?�?�? �?�?�?�?�? �?�?�? �?�?�?�?�?�?");
    if (!message.trim()) return alert("�?�?�?�? �?�?�?�?�? �?�?�?�?�?�?�?");
    if (bulkMode && !file) return alert("�?�?�?�? �?�?�? �?�?�? Excel �?�?�?�?�? �?�?�? ��?�?�?�? �?�?�?�?�?�?�?");

    try {
      const res = await fetch(`${apiUrl}api/RasaelCP/SendSMSToPhoneNumber`, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          Authorization: `Bearer ${localStorage.getItem("token")}`,
        },
        body: JSON.stringify({
          token: localStorage.getItem("rasaelToken"),
          phoneNumber: phone,
          message: message,
        }),
      });

      if (!res.ok) throw new Error((await res.text()) || "�?�?�? �?�?�?�?�? �?�?�?�?�?�?�?");

      alert("�?�? �?�?�?�?�? �?�?�?�?�?�?�?!");
      setMessage("");
      setPhone("");
      setFile(null);
      setBulkMode(false);
      fetchHistory();
    } catch (err) {
      alert(err.message);
    }
  };

  const fetchHistory = async () => {
    const userData = JSON.parse(localStorage.getItem("userData") || "{}");
    const today = new Date();
    const yesterday = new Date(today);
    yesterday.setDate(today.getDate() - 1);
    const tomorrow = new Date(today);
    tomorrow.setDate(today.getDate() + 1);

    const fromDate = filterFromDate || yesterday.toISOString().split("T")[0];
    const toDate = filterToDate || tomorrow.toISOString().split("T")[0];

    try {
      const res = await fetch(`${apiUrl}api/RasaelCP/GetRasaelHistorySelect`, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          Authorization: `Bearer ${localStorage.getItem("token")}`,
        },
        body: JSON.stringify({
          clientId: userData.userID,
          phoneNo: filterPhone,
          fdate: fromDate,
          tdate: toDate,
        }),
      });

      if (res.ok) {
        const data = await res.json();
        setHistory(data);
      }
    } catch (err) {
      console.error(err);
    }
  };

  useEffect(() => {
    const today = new Date();
    const yesterday = new Date(today);
    yesterday.setDate(today.getDate() - 1);
    const tomorrow = new Date(today);
    tomorrow.setDate(today.getDate() + 1);

    if (!filterFromDate) setFilterFromDate(yesterday.toISOString().split("T")[0]);
    if (!filterToDate) setFilterToDate(tomorrow.toISOString().split("T")[0]);

    fetchHistory();
  }, []);

  const filteredHistory = history.filter((h) => {
    let pass = true;
    if (filterPhone && !h.phoneNo?.includes(filterPhone)) pass = false;
    if (filterFromDate && new Date(h.createdAt) < new Date(filterFromDate)) pass = false;
    if (filterToDate && new Date(h.createdAt) > new Date(filterToDate)) pass = false;
    return pass;
  });

  return (
    <div className="space-y-10 p-4 text-right">
      <div className="space-y-6 rounded-3xl bg-white p-6 shadow-card lg:flex lg:gap-6">
        <form className="flex-1 space-y-6" onSubmit={handleSubmit}>
          <div className="space-y-3">
            <div className="flex items-center justify-between text-sm font-semibold text-gray-600">
              <label className="text-sm font-semibold text-gray-600">
                {bulkMode ? "�?�?�?�?�? ��?�?�?�? �?�?�?�?�?�?�?:" : "�?�?�? �?�?�?�?�?�?:"}
              </label>
              <label className="flex items-center gap-2 text-xs font-normal text-gray-500">
                <input
                  type="checkbox"
                  checked={bulkMode}
                  onChange={(e) => setBulkMode(e.target.checked)}
                />
                �?�?�?�?�? �?�?�? ��?�?�?�? �?�?�?�?�?�?
              </label>
            </div>

            {!bulkMode ? (
              <input
                type="tel"
                value={phone}
                onChange={(e) => setPhone(e.target.value)}
                placeholder="�?��?�?: 218912345678"
                className="w-full rounded-2xl border border-gray-200 px-4 py-3 text-base text-gray-700 focus:border-brand-primary focus:outline-none focus:ring-2 focus:ring-brand-primary/20"
              />
            ) : (
              <input
                type="file"
                accept=".xls,.xlsx"
                onChange={handleFileChange}
                className="w-full rounded-2xl border border-gray-200 px-4 py-3 text-base text-gray-700 focus:border-brand-primary focus:outline-none focus:ring-2 focus:ring-brand-primary/20"
              />
            )}
            {file && (
              <p className="text-sm text-brand-primary">
                �?�?�?�?�? �?�?�?�?�?�?: <strong>{file.name}</strong>
              </p>
            )}
          </div>

          <div className="space-y-3">
            <label className="text-sm font-semibold text-gray-600">�?�� �?�?�?�?�?�?�?:</label>
            <textarea
              value={message}
              onChange={(e) => setMessage(e.target.value.slice(0, maxLength))}
              rows={6}
              className="w-full rounded-2xl border border-gray-200 px-4 py-3 text-base text-gray-700 focus:border-brand-primary focus:outline-none focus:ring-2 focus:ring-brand-primary/20"
              placeholder="�?�?�?�? �?�?�?�?�?�? �?�?�?..."
            />
            <div className="text-left text-xs text-gray-500">
              {message.length} / {maxLength}
            </div>
          </div>

          <button
            type="submit"
            className="w-full rounded-2xl bg-brand-primary px-4 py-3 text-white transition hover:bg-brand-dark"
          >
            �?�?�?�?�?
          </button>
        </form>

        <div className="mt-6 flex-1 rounded-3xl border border-gray-200 bg-white shadow-inner lg:mt-0">
          <div className="sticky top-0 rounded-t-3xl bg-brand-primary px-4 py-3 text-sm font-semibold text-white">
            �?�?�?�?�?�� �?�?�?��?�?�?�?
          </div>
          <div className="flex-1 space-y-4 overflow-y-auto p-6 text-sm leading-relaxed text-gray-600 max-h-[28rem]">
            <PolicyList />
          </div>
          <div className="border-t border-brand-primary/20 bg-brand-primary/90 px-4 py-3 text-sm text-white">
            <label className="flex items-center gap-2 font-semibold">
              <input
                type="checkbox"
                checked={policyAccepted}
                onChange={(e) => setPolicyAccepted(e.target.checked)}
              />
              ��?�?�?�? �?�?�? �?�?�?�?�?�� �?�?�?��?�?�?�?
            </label>
          </div>
        </div>
      </div>

      <div className="space-y-4 rounded-3xl bg-white p-6 shadow-card">
        <h4 className="text-2xl font-bold text-brand-primary">�?�?�?�?�? �?�?�?�?�? �?�?�?�?�?</h4>
        <div className="flex flex-col gap-4 md:flex-row md:flex-wrap">
          <FilterField
            label="�?�?�? �?�?�?�?�?�?:"
            type="text"
            value={filterPhone}
            placeholder="�?�?� �?�?�?�? �?�?�?�?�?�?"
            onChange={setFilterPhone}
          />
          <FilterField
            label="�?�? �?�?�?�?�?:"
            type="date"
            value={filterFromDate}
            onChange={setFilterFromDate}
          />
          <FilterField
            label="�?�?�? �?�?�?�?�?:"
            type="date"
            value={filterToDate}
            onChange={setFilterToDate}
          />
          <button
            type="button"
            onClick={fetchHistory}
            className="inline-flex items-center justify-center gap-2 rounded-2xl bg-brand